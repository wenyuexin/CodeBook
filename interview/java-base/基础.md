# Java基础

### 代码块

在Java中，代码块可以分为以下几种：

1. 静态代码块：用static修饰，当JVM在加载类时，只会执行一次静态代码块。

2. 实例代码块（构造代码块）：直接在类中定义，没有static修饰，每次创建对象时都会执行，并且在构造函数之前。

3. 构造函数：用于初始化对象。

4. 普通代码块：在方法中定义的代码块，限制变量的生命周期。

**执行顺序**：静态代码块 > 实例代码块 > 构造函数 > 普通代码块。

**有继承关系的两个类的执行顺序**：父类静态代码块 > 子类静态代码块 > 父类实例代码块 > 父类构造函数 >  子类实例代码块 > 子类构造器

```java
public class CodeBlockTest {
    static {
        System.out.println("静态代码块");
    }

    {
        System.out.println("实例代码块/构造代码块");
    }

    public CodeBlockTest() {
        System.out.println("构造函数");
    }

    public void test() {
        System.out.println("普通代码块");
    }
}
```

静态代码块

- 静态代码块使用static关键字进行修饰

- 静态代码块的内部可以有输出语句

- 静态代码块在类加载时执行，并且只会执行一次 (因为一个类只会加载一次)

- 静态代码块用于初始化类，但是初始化的是类的静态属性，类的非静态属性是无法在静态代码块中被初始化的

- 如果一个类中存在多个静态代码块，那么会按照这些静态代码块的定义顺序来执行。

- 如果静态代码块中存在调用，那么只能调用静态的属性和方法，不能调用非静态的属性和方法

### 接口和抽象类

**语法层面**

1）抽象类可以提供成员方法的实现细节，而接口中只能存在public abstract 方法； 2）抽象类中的成员变量可以是各种类型的，而接口中的成员变量只能是public static final类型的； 3）接口中不能含有静态代码块以及静态方法，而抽象类可以有静态代码块和静态方法； 4）一个类只能继承一个抽象类，而一个类却可以实现多个接口。

**关于接口的default方法**

在java8以后，接口中可以添加使用default或者static修饰的方法。default修饰方法只能在接口中使用，在接口种被default标记的方法为普通方法，可以直接写方法体。

1）实现类会继承接口中的default方法

2）如果一个类同时实现接口A和B，接口A和B中有相同的default方法，这时，该类必须重写接口中的default方法

3）如果子类继承父类，父类中有b方法，该子类同时实现的接口中也有b方法（被default修饰），那么子类会继承父类的b方法而不是继承接口中的b方法

### 重写Override和重载Overload

===

问题1：重写和重载有什么区别 yes

问题2：入参相同 回参类型不同的两个方法能构成重载吗？不能 yes

问题3：怎么防止方法被重写？加final  == 访问权限

问题3：static方法能被重写吗？不能，但能重新声明 yes

1）重载

一个类中定义了多个方法名相同,而他们的参数的数量不同或数量相同而类型和次序不同

两同：同一个类、方法名相同

三不同：参数个数、参数类型、参数顺序

- 被重载的方法必须改变参数列表(参数个数或类型不一样)；

- 被重载的方法可以改变返回类型；

- 被重载的方法可以改变访问修饰符；

- 被重载的方法可以声明新的或更广的检查异常；

- 方法能够在同一个类中或者在一个子类中被重载。

- 无法以返回值类型作为重载函数的区分标准。

注意，不能通过返回类型区分，因为在调用目标方法时，是无法指定返回值类型信息的，这个时候编译器并不知道你要调用哪个函数

2）重写

在子类存在与父类方法的名字相同，参数的个数与类型一样，返回值也一样的方法。

- 参数列表与被重写方法的参数列表必须完全相同。

- 返回类型与被重写方法的返回类型可以不相同，但是必须是父类返回值的派生类（java5 及更早版本返回类型要一样，java7 及更高版本可以不同）。

- 访问权限不能比父类中被重写的方法的访问权限更低。例如：如果父类的一个方法被声明为 public，那么在子类中重写该方法就不能声明为 protected。

- 父类的成员方法只能被它的子类重写。

- 声明为 final 的方法不能被重写。

- 声明为 static 的方法不能被重写，但是能够被再次声明。

- 子类和父类在同一个包中，那么子类可以重写父类所有方法，除了声明为 private 和 final 的方法。

- 子类和父类不在同一个包中，那么子类只能够重写父类的声明为 public 和 protected 的非 final 方法。

- 重写的方法能够抛出任何非强制异常，无论被重写的方法是否抛出异常。但是，重写的方法不能抛出新的强制性异常，或者比被重写方法声明的更广泛的强制性异常，反之则可以。

- 构造方法不能被重写。

- 如果不能继承一个类，则不能重写该类的方法。

### hashcode和equals什么关系

- hashCode主要用于提升查询效率提高哈希表性能，来确定在散列结构中对象的存储地址

- 重写equals()必须重写hashCode()

- 哈希存储结构中，添加元素重复性校验的标准就是先检查hashCode值，后判断equals()

- 两个对象equals(相等，hashcode()必定相等

- 两个对象hashcode()不等，equals()必定也不等

- 两个对象hashcode()相等，对象不一定相等，需要通过equals()进一步判断。

### == 和 equals() 的区别是什么？

- == 比较的是对象的引用地址（内存地址），对于基本数据类型比较的是值

- equals() 是 Object 类的方法，默认实现与 == 相同，但通常会被重写用于比较对象的内容

### String、StringBuffer和StringBuilder的区别

1）String类是不可变类，即一旦一个String对象被创建以后，包含在这个对象中的字符序列是不可改变的，直至这个对象被销毁。

2）tringBuffer对象则代表一个字符序列可变的字符串，当一个StringBuffer被创建以后，通过StringBuffer提供的append()、insert()、reverse()、setCharAt()、setLength()等方法可以改变这个字符串对象的字符序列。一旦通过StringBuffer生成了最终想要的字符串，就可以调用它的toString()方法将其转换为一个String对象。

3）StringBuilder类也代表可变字符串对象。实际上，StringBuilder和StringBuffer基本相似，两个类的构造器和方法也基本相同。不同的是：**StringBuffer是线程安全的，而StringBuilder则没有实现线程安全功能，所以性能略高。**

4）StringBuffer类中的方法都添加了**synchronized关键字**，也就是给这个方法添加了一个锁，用来保证线程安全。

5）**Java9的改进**：Java9改进了字符串（包括String、StringBuffer、StringBuilder）的实现。在Java9以前字符串采用char\[\]数组来保存字符，因此字符串的每个字符占2字节；而Java9的字符串采用byte\[\]数组再加一个encoding-flag字段来保存字符，因此字符串的每个字符只占1字节。所以Java9的字符串更加节省空间，字符串的功能方法也没有受到影响。

### 说常见的接口的抽象类、常用的集合类

List, Map, Set接口以及具体的子类，随便说几个

### ArrayList 和 LinkedList 的区别是什么？

- ArrayList 基于动态数组实现，随机访问快(O(1))，但插入删除慢(O(n))

- LinkedList 基于双向链表实现，插入删除快(O(1))，但随机访问慢(O(n))

- ArrayList 需要连续内存空间，LinkedList 不需要

### ArrayList、Vector、LinkedList是什么，有什么区别

ArrayList 和Vector都是使用数组方式存储数据，此数组元素数大于实际存储的数据以便增加和插入元素，它们都允许直接按序号索引元素，但是插入元素要涉及数组元素移动等内存操作，所以索引数据快而插入数据慢，Vector中的方法由于添加了synchronized修饰，因此Vector是线程安全的容器，但性能上较ArrayList差，因此已经是Java中的遗留容器。

LinkedList使用**双向链表**实现存储（将内存中零散的内存单元通过附加的引用关联起来，形成一个可以按序号索引的线性结构，这种链式存储方式与数组的连续存储方式相比，内存的利用率更高），按序号索引数据需要进行前向或后向遍历，但是插入数据时只需要记录本项的前后项即可，所以插入速度较快。

### HashMap实现

- 通过 key 的 hashCode() 计算哈希值，确定数组下标

- 解决哈希冲突使用链地址法，当链表长度超过8时转为红黑树

- 默认负载因子0.75，当元素数量超过容量\*负载因子时进行扩容

java8之前，数组+链表 或者 数组+红黑树，链表长大于8时 转红黑树

java8之后，当链表长度超过8时，会先判断数组长度是否大于等于64，如果是 则将链表转成红黑树

### ConcurrentHashMap实现

=== 知道一点 segment  cas

**JDK1.7**

采用分段锁的机制，实现并发的更新操作，底层采用数组+链表的存储结构，包括两个核心静态内部类Segment 和 HashEntry。

- Segment 继承 ReentrantLock（重入锁） 用来充当锁的角色，每个 Segment 对象守护每个散列映射表的若干个桶；

- HashEntry用来封装映射表的键值对

- 每个桶是由若干个 HashEntry 对象链接起来的链表。

总结：Segment数组 + HashEntry数组 + 链表

**JDK 1.8**

采用Node + CAS + Synchronized来保证并发安全。**不再使用 Segment**，直接用 **table 数组**存储键值对；当 HashEntry 对象组成的链表长度超过 TREEIFY_THRESHOLD 时，**链表转换为红黑树**，提升性能。底层变更为**数组 + 链表 + 红黑树**。

总结：数组 + (链表/红黑树)

### Map、HashMap、HashTable、linkedHashMap、SynchronizedMap、concurrentHashMap的对比

=== Map、HashMap、concurrentHashMap

=== 怎么将hashmap对象变成线程安全的

**Map、HashMap、HashTable、ConcurrentHashMap的区别**‌主要体现在以下几个方面：

1. ‌**线程安全性**‌：

   - ‌**HashMap**‌：非线程安全，多线程环境下需要外部同步控制，可以通过`Collections.synchronizedMap()`来包装HashMap‌12。

   - ‌**Hashtable**‌：线程安全，所有方法都使用`synchronized`关键字进行同步，保证了多线程环境下的安全性，但同步机制会带来性能开销‌12。

   - ‌**ConcurrentHashMap**‌：线程安全，通过分段锁（Segmented Locking）机制实现并发控制，允许多个线程并发访问不同的段，提高了并发性能‌12。

2. ‌**同步机制**‌：

   - ‌**HashMap**‌：没有同步机制，需要在多线程环境下自行处理同步‌12。

   - ‌**Hashtable**‌：使用全表锁定的机制，每次操作都必须获取整个对象的锁，同步机制较为简单但会影响性能‌12。

   - ‌**ConcurrentHashMap**‌：使用分段锁机制，每个段独立锁定，允许多个线程同时访问不同的段，提高了并发性能‌12。

3. ‌**性能比较**‌：

   - ‌**HashMap**‌：在单线程环境下性能优于Hashtable和ConcurrentHashMap，但在多线程环境下需要外部同步控制‌12。

   - ‌**Hashtable**‌：虽然线程安全，但由于全表锁定机制，在高并发环境下性能较差‌12。

   - ‌**ConcurrentHashMap**‌：在高并发场景下表现优异，通过细粒度的分段锁机制大幅减少了锁的竞争和开销，提高了并发性能‌12。

4. ‌**底层数据结构**‌：

   - ‌**HashMap**‌：基于哈希表的实现，JDK 1.8版本后引入了红黑树优化链表过长的问题‌。

   - ‌**Hashtable**‌：同样是基于哈希表的实现，不允许使用null键和值‌4。

   - ‌**ConcurrentHashMap**‌：通过分段锁机制优化了哈希表的并发性能‌3。

5. ‌**历史背景和继承关系**‌：

   - ‌**HashMap**‌：继承自`AbstractMap`类，实现了`Map`接口。

   - ‌**Hashtable**‌：继承自`Dictionary`类，并实现了`Map`接口，是早期Java集合框架中的一个类‌。

   - ‌**ConcurrentHashMap**‌：继承自`AbstractMap`类，实现了`ConcurrentMap`接口，是Java 5引入的线程安全的HashMap‌。

**HashMap 与 HashTable** 1、HashMap key value 都可以是null，Hashtable 不可以 2、HashMap 线程不安全，Hashtable 线程安全

**SynchronizedMap 和 ConcurrentHashMap**

1、SynchronizedMap来自Collections工具类，是采用全部加锁，相当于包装了一层，内部加完锁再调用hashmap，返回的也是一个SynchronizedMap 会保证原来map中的顺序 2、ConcurrentHashMap 采用重写的方式，

1.8之前，采用分段锁技术，只锁相关的bucket，不相关部分可以并发，提高了效率，但是是把map中的值重写到HashEntry\[\]中，不再是一个map， 另外也不会保持原先map中的顺序。

1.8之后，取消了分段锁的概念，采用CAS实现。

**Hashtable 与 Collections.synchronizedMap(HashMap) 的区别**

1、默认 Hashtable 和 synchrnizedMap 都是锁 类实例，synchrnizedMap 可以选择锁其他的 Object（mutex）

2、Hashtable 的 synchronized 是方法级别的；synchrnizedMap 的 synchronized 的代码块级别的

3、两者性能相近，但是 synchrnizedMap 可以用 null 作为 key 和 value

### 异常处理

正确  详细

Thorwable

RuntimeException\\n|——ClassCastException多态中可以使用instanceof 进行规避\\n|——ArithmeticException进行if判断，吐过除数为0进行return\\n|——NullPointerException进行if判断是否为nullt\\n|——ArrayIndexOutBondsExcetion使用数组length属性以避免数组越界。

- 非运行时异常必须catch或throw

**finally关键字**

- finally前return了，finally里的代码是否还会执行？是的

- try中return时会把return缓存，finally中对return变量的操作不影响

- 当try和finally里都有return时，会忽略try的return，而使用finally的return

### 什么是 Java 反射？它有什么优缺点？

**答案**： 反射是 Java 在运行时检查、修改类和对象的能力。

优点：

- 动态创建对象和调用方法

- 实现通用框架和工具（如Spring IOC）

缺点：

- 性能开销大

- 破坏封装性

- 安全性问题

### 如何通过反射获取类的私有方法并调用？

```java
Class<?> clazz = Class.forName("com.example.MyClass");
Method privateMethod = clazz.getDeclaredMethod("privateMethod");
privateMethod.setAccessible(true);  // 设置可访问
Object result = privateMethod.invoke(clazz.newInstance());
```

### Java 中有哪些 IO 流？它们有什么区别？

**答案**：

- 按方向：输入流(InputStream/Reader)和输出流(OutputStream/Writer)

- 按单位：字节流(InputStream/OutputStream)和字符流(Reader/Writer)

- 按功能：节点流(直接操作数据源)和处理流(包装其他流增强功能)

### NIO 和传统 IO 的区别是什么？

**答案**：

- 传统IO是面向流的、阻塞的；NIO是面向缓冲区的、非阻塞的

- NIO引入了Channel、Selector和Buffer概念

- NIO适合高并发场景，传统IO适合连接数少但数据传输量大的场景

# Java新特性

### java17

相对于 JDK8 的新特性。

- 类型推断

```java
static void test() {
    var one = 1;
    var o = new Object();
}
```

- instanceof

```
static void test(Object o) {
    if ((o instanceof String s) && s.length() > 3) {
        System.out.println(s);
    } else {
        System.out.println("Not a string");
    }
}
```

- switch

JDK20 以下，还是预览功能，如要使用，请在编译和VM加上 --enable-preview

- stream API 优化

`Stream.collect(Collectors.toList()) => Stream.toList() // !不可变列表`

- optional

```
public static void testOptional(Optional<Object> optional) {
    // 1. ifPresentOrElse
    optional.ifPresentOrElse(System.out::println, () -> System.out.println("null"));
    // 2. 生成新的Optional
    optional.or(() -> Optional.ofNullable(new Object()));
    // 3. 转为 stream
    optional.stream().collect(Collectors.toSet());
}
```

- 文本块

```java
public static final String getHtmlTextInJDK17() {
    return """
            <html>
              <body>
                <p>Hello, world</p>
              </body>
            </html>
          """;
}
```

- 集合工厂方法

```
// 不可变 List
List.of(1,2,3);
// 不可变 Set
Set.of(1,2,3);
// 不可变map
Map.of("k1", "v1", "k2", "v2");
// from entry
Map.ofEntries(Map.of().entrySet().toArray(new Map.Entry[0]));
```

- 私有接口方法

```
public interface HelloService {
    public void sayHello();
    // 默认方法
    default void saySomething(){
        sayEngHello();
        sayHello();
    };
    // 私有方法
    private void sayEngHello(){
        System.out.println("Hello!");
    }
}
```

- 标准Java HTTP Client

比 Apache HttpClient 更好用的官方非阻塞 HttpClient

```
HttpClient client = HttpClient.newHttpClient();
HttpRequest request = HttpRequest.newBuilder()
      .uri(URI.create("http://openjdk.java.net/"))
      .build();
client.sendAsync(request, BodyHandlers.ofString())
      .thenApply(HttpResponse::body)
      .thenAccept(System.out::println)
      .join();
```

- 空指针优化

提示精确到字段的空指针提示

```
Exception in thread "main" java.lang.NullPointerException
    at Prog.main(Prog.java:5)

    ||   
    \/

Exception in thread "main" java.lang.NullPointerException: 
        Cannot read field "c" because "a.b" is null
    at Prog.main(Prog.java:5)
```

- recored

类似 lombok @Data 的功能。

```
record StuffRecord (String name, int age, double salary) {

    public void printBaseInfo() {
        System.out.println(name + ", age=" + age + ", salary=" + salary);
    }
}

public static void recordTest() {
    var stuffRecord = new StuffRecord("A-man", 18, 2000000);
    var name = stuffRecord.name();
    var age = stuffRecord.age();
    val salary = stuffRecord.salary();
    stuffRecord.printBaseInfo();
}
```

- 其他

字符串添加 isBlank()等方法。

Sealed Classes 密封类，通过sealed关键字修饰抽象类限定只允许指定的子类才可以实现或继承抽象类，避免抽象类被滥用

G1 作为默认垃圾回收器，并进行改进，添加更为强大的 ZGC (STW 时间与堆大小无关)。